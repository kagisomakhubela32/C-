 #include "iostream"
 #include "cstring"
 #include "conio.h"
 
 using namespace std;
 
 void coutChar(int cout, string message)
 {
 	int count;
	string message;
	 
	int(message.length());
 }
 
 int main()
 {
	string message;
	int count;
		
	cout <<"Please enter message: "<<endl;
	getline(cin,message);
	
	cout <<"The total number of charectors in the message is = "<<coutChar(cout, message)<<endl; 
	
	//cout <<int(message.length());
	 
	_getch();
	return 0;	
 }
